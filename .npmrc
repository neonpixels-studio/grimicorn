# Fail install/ci loudly instead of warning when the local Node doesn't
# satisfy the "engines.node" constraint in package.json (see the
# Requirements section in README.md). Also enforces engines.node declared
# by dependencies, not just this project's own constraint.
engine-strict=true
